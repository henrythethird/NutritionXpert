{% extends 'base.html.twig' %}

{% block body %}
    <h2>New Ingredient</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-primary" formnovalidate>Save Changes</button>
    <a class="btn btn-default" href="{{ path('ingredient_list') }}">Cancel</a>
    {{ form_end(form) }}
{% endblock %}