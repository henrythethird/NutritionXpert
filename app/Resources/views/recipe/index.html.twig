{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center">Recipes</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Rating</th>
            </tr>
        </thead>
        <tbody>
            {% for recipe in recipes %}
            <tr>
                <td>{{ recipe.id }}</td>
                <td><a href="{{ path('recipe_show', { 'recipeName': recipe.name }) }}">{{ recipe.name }}</a></td>
                <td>{{ recipe.rating }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}